# =================================================
# 🎬 Auto Video Editor - Configurações de Ambiente
# =================================================
# Copie este arquivo para .env e configure os valores:
#   cp env.sample .env
# 
# SEGURANÇA: O arquivo .env NUNCA deve ser commitado!
# =================================================

# ==================== API KEYS ====================
# Chave da API do Google Gemini para correção de texto com IA
# Obtenha em: https://aistudio.google.com/apikey
GEMINI_API_KEY=sua_chave_aqui

# ==================== SERVIDOR WEB ====================
# Porta do servidor (padrão: 3001)
PORT=3001

# Host do servidor (0.0.0.0 permite acesso na rede local)
HOST=0.0.0.0

# Modo debug (NUNCA use 'true' em produção!)
FLASK_DEBUG=false

# Chave secreta do Flask para sessões
# IMPORTANTE: Gere uma chave aleatória para produção!
# Comando: python -c "import secrets; print(secrets.token_hex(32))"
FLASK_SECRET_KEY=

# ==================== AUTENTICAÇÃO ====================
# 🔐 PROTEÇÃO COM SENHA (para expor na internet)
# 
# OPÇÃO 1: Senha em texto (mais fácil, converte para hash automaticamente)
# ⚠️ Recomendado apenas para testes - em produção use ACCESS_PASSWORD_HASH
ACCESS_PASSWORD=

# OPÇÃO 2: Hash da senha (mais seguro)
# Gere o hash com o comando:
#   python -c "from werkzeug.security import generate_password_hash; print(generate_password_hash('SUA_SENHA_AQUI'))"
ACCESS_PASSWORD_HASH=

# Tempo de sessão em horas (padrão: 24)
SESSION_LIFETIME_HOURS=24

# ==================== WHISPER ====================
# Modelo padrão do Whisper (tiny, base, small, medium, large)
WHISPER_MODEL=small

# Idioma padrão para transcrição
WHISPER_LANGUAGE=pt

