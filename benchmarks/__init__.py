"""Benchmarks — experiments comparing our pipeline against other approaches.

Not part of the shipped CLI: everything here is run by hand, writes into
`workspace/bench_*` and reports into `benchmarks/reports/`.
"""
